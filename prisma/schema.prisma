generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./rekuebox.db"
}

model contacts {
  contact_id Int    @id @default(autoincrement())
  first_name String
  last_name  String
  email      String @unique(map: "sqlite_autoindex_contacts_1")
  phone      String @unique(map: "sqlite_autoindex_contacts_2")
}
